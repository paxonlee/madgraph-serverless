[project]
name = "madgraph-serverless"
version = "0.1.5"
description = "Run MadGraph5_aMC@NLO on Runpod Serverless"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "pexpect",
  "uvicorn",
]
